
list dstList = {1}

from 200 every 0.035 do {
var value = 10
packet fakePacket
create(fakePacket, "APP.type", 0000)
change(fakePacket, "APP.data", value)
put(fakePacket, dstList, TX, FALSE, 0)
value += 1
}

